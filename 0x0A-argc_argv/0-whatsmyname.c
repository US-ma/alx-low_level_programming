#include <stdio.h>
#include "main.h"

/**
 * main - main
 * @argc: length of argument
 * @argv: argument array
 * Return: Always 0 (Success)
 */
int main(int argc, char **argv)
{
	printf("%s\n", argv[0]);
	return (0);
}
